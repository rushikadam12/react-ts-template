# React Template (RK)

## Features
- React + TypeScript + Vite
- TailwindCSS
- ESLint (strict rules + boundaries)
- Zustand (UI state)
- React Query (API state)
- Feature-based architecture

## Setup

```bash
npm install
cp .env.example .env
npm run dev# react-ts-template
